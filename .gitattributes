* text=auto

# Large binaries stay in Git LFS to keep clones small.
*.onnx filter=lfs diff=lfs merge=lfs -text
selfdrive/car/tests/test_models_segs.txt filter=lfs diff=lfs merge=lfs -text
system/hardware/tici/updater filter=lfs diff=lfs merge=lfs -text

# Image / font / sound types are shipped as REAL blobs in this fork. Declaring
# them as LFS while the committed blob is real content made git-lfs report
# phantom "modified" files ("Encountered files that should have been pointers,
# but weren't") and blocked git pull on agnos. So they are NOT LFS by type.
# -text also keeps them out of EOL renormalization.
*.svg -filter -diff -merge -text
*.png -filter -diff -merge -text
*.gif -filter -diff -merge -text
*.ttf -filter -diff -merge -text
*.otf -filter -diff -merge -text
*.wav -filter -diff -merge -text

# The four assets below are shipped as REAL blobs in this fork, exactly like the
# image / font / sound types above. Storing them as LFS pointers meant a plain
# `git clone` with NO git-lfs installed checked out the pointer text instead of
# the file. The committed blobs are byte-identical to the LFS objects (sha256
# verified), so every clone path now yields the same result: the actual file.
# `-filter` also stops git-lfs from ever treating them as pointers again.
docs/assets/comma-logo.png -filter -diff -merge -text
openpilot/tools/cabana/assets/cabana-icon.png -filter -diff -merge -text
openpilot/selfdrive/carrot/web/assets/alert_camera.svg -filter -diff -merge -text
openpilot/selfdrive/carrot/web/assets/alert_police.svg -filter -diff -merge -text

# dmonitoring model was already shipped as a real blob (supercombo stays LFS).
openpilot/selfdrive/modeld/models/dmonitoring_model.onnx -filter -diff -merge -text
